import React from 'react'
import { PROJECT_NAME } from '../../metadata'

type Props = {}

const HomePage = (props: Props) => {
  return (
    <div>Landing Page { PROJECT_NAME } </div>
  )
}

export default HomePage